# Unversioned minimal requirements
# Can force Anaconda env install with: while read requirement; do conda install --yes $requirement; done < requirements_minimal_unversioned.txt
# See https://stackoverflow.com/questions/35802939/install-only-available-packages-using-conda-install-yes-file-requirements-t
#
# Should also work with channels added, E.g. "conda create --name ePSproc --file ePSproc/requirements.txt --channel default --channel conda-forge", see https://epsproc.readthedocs.io/en/latest/etc/installation_notes_051120.html#using-conda
#
# 12/06/24: some updates to use with quick install script, based on recent Docker builds.
#
# 12/08/21: this version has Xarray & Seaborn pinned to versions prior to package breaking changes, to be fixed!
#           UPDATE 27/10/22: Xarray up to v2022.3.0 (March 2022) now OK, but IO issues with newer versions.
# 20/04/21: unversioned requirements testing

numpy_quaternion
# Numpy 1.26 breaks some epsproc.IO code in dumpIdySegsParseX(), related to array size or ordering?
numpy<1.24
scipy
xarray<=2022.3.0
#==0.15.0
pandas

# Matplotlib v3.9 breaks compatibility with Seaborn 0.9. TODO: update Seaborn usage!
matplotlib<3.9   
seaborn==0.9.0

spherical_functions

# Optional, but included here for now
numba
pyvista
cclib
holoviews
plotly
hvplot

# For Plotly Jupyter Lab rendering
jupyter-dash 

# Others which should be optional...
mpmath
wget
